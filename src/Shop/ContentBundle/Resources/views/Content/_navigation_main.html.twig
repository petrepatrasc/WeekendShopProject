{% block navigation %}
    <div class="row"><!-- start nav -->
        <div class="span12">
            <div class="navbar">
                <div class="navbar-inner">
                    <div class="container" style="width: auto;">
                        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                            <span class="icon-bar"></span>

                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </a>

                        <div class="nav-collapse">
                            <ul class="nav">

                            {% for category in categories %}
                                <li>
                                    <a href="{{ path('shop_category_listing', {'id': category.id, 'name' : category.name|lower|replace({' ' : '-'})}) }}">{{ category.name }}</a>
                                </li>
                            {% endfor %}

                            </ul>

                        </div>
                        <!-- /.nav-collapse -->
                    </div>
                </div>
                <!-- /navbar-inner -->
            </div>
            <!-- /navbar -->
        </div>
    </div><!-- end nav -->
{% endblock %}